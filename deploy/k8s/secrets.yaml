# Placeholders: replace before applying, or generate this Secret from your
# secret manager instead. Never commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: netflow-collector
type: Opaque
stringData:
  # openssl rand -hex 32, comma-separated for rotation
  NFC_API_KEYS: "replace-me"
  NFC_CLICKHOUSE_DSN: "clickhouse://netflow:replace-me@clickhouse.netflow:9000/netflow"
